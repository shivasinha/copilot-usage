{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Copilot Proxy",
      "type": "shell",
      "command": "Get-Process mitmdump -ErrorAction SilentlyContinue | Stop-Process -Force; Start-Sleep -Milliseconds 500; mitmdump --listen-port 18080 -s proxy_poc.py",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "isBackground": true,
      "problemMatcher": [],
      "runOptions": {
        // "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "silent",
        "panel": "dedicated",
        "label": "Copilot Proxy",
        "close": false
      }
    }
  ]
}
